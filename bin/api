#!/usr/bin/env bash
set -e
DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
export OPENSSL_CONF="$DIR/../openssl_legacy.cnf"
PORT="${PORT:-8081}"
exec php -S "127.0.0.1:${PORT}" "$DIR/../public/api.php"
